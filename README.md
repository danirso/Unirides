# sistema-de-carona

pra testar o docker:
clone o repositório
rode o comando:
'''docker compose up -d --build'''
depois disso, entre nos links
localhost:8080 <-phpmyadmin
localhost:3000 <-nosso site
