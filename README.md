# sistema-de-carona

pra acessar o sistema:
- clone o repositório
- rode o comando:
``docker compose up -d --build``
- entre nos links:
- ``localhost:8080`` <-phpmyadmin
- ``localhost:3000`` <-nosso site
